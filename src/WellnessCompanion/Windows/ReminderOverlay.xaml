<Window x:Class="WellnessCompanion.Windows.ReminderOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="560"
        Height="430"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Storyboard x:Key="WaterPulse" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="WaterDrop" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="1" To="1.09" Duration="0:0:1.15" />
            <DoubleAnimation Storyboard.TargetName="WaterDrop" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="1" To="1.09" Duration="0:0:1.15" />
            <DoubleAnimation Storyboard.TargetName="DropGlow" Storyboard.TargetProperty="Opacity" From="0.25" To="0.8" Duration="0:0:1.15" />
        </Storyboard>
    </Window.Resources>
    <Border CornerRadius="28"
            Background="#F0091426"
            BorderBrush="#38577A"
            BorderThickness="1"
            Padding="36">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid x:Name="WaterVisual" Width="112" Height="112" HorizontalAlignment="Center">
                <Ellipse x:Name="DropGlow" Width="104" Height="104" Fill="#2563EB" Opacity="0.32" />
                <Path x:Name="WaterDrop" Data="M56,8 C56,8 23,44 23,67 A33,33 0 1 0 89,67 C89,44 56,8 56,8 Z" Stretch="Uniform" Fill="#38BDF8" Stroke="#BAE6FD" StrokeThickness="2" RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform><ScaleTransform /></Path.RenderTransform>
                </Path>
                <Path Data="M41,64 C46,53 54,47 67,43 C61,51 58,59 58,67 C58,75 61,82 66,88 C53,85 44,77 41,64 Z" Fill="#E0F2FE" Opacity="0.72" />
            </Grid>
            <TextBlock x:Name="EmojiText" FontSize="64" HorizontalAlignment="Center" TextAlignment="Center" Visibility="Collapsed"/>

            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText"
                           Foreground="#7DD3FC"
                           FontSize="30"
                           FontWeight="Bold"
                           TextAlignment="Center"/>
                <TextBlock x:Name="SubtitleText"
                           Foreground="#CBD5E1"
                           FontSize="16"
                           Margin="0,10,0,18"
                           TextAlignment="Center"
                           TextWrapping="Wrap"/>
                <TextBlock x:Name="CountdownText"
                           Foreground="White"
                           FontSize="44"
                           FontWeight="SemiBold"
                           TextAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="2"
                        x:Name="FoodButtons"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Visibility="Collapsed">
                <Button Content="10 min" Margin="4" Padding="14,8" Click="Shorten10_Click"/>
                <Button Content="15 min" Margin="4" Padding="14,8" Click="Shorten15_Click"/>
                <Button Content="20 min" Margin="4" Padding="14,8" Click="Shorten20_Click"/>
                <Button Content="Finish" Margin="4" Padding="14,8" Click="Finish_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
